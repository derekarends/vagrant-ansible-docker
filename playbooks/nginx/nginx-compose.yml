version: "3"
services:
  nginx:
    image: nginx
    deploy:
      replicas: 2
    ports:
      - "80:80"
    volumes:
      - "/var/run/docker.sock:/tmp/docker.sock:ro"
      - "../files/nginx.conf:/etc/nginx/conf.d/nginx.conf"
    networks:
      - webnet
networks:
  webnet: